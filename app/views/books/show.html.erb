<h2>Book Detail</h2>

<table>
  <tbody>
    <tr>
      <%= attachment_image_tag @book, :image %>
      <td><%= @book.user.name %></td>
      <td><%= @book.title %></td>
      <td><%= @book.body %></td>
    </tr>
  </tbody>
</table>
